1.) HTML :- Hyper Text Markup Language :- -> it uses tags to define element.
	
-> use to create web pages.

.<!DOCTYPE html> --> browser ko batane ke liye ki below code are html document
                -->aur hmm html 5 version use kar rhe hai

.<head> tag --> iske under hmm meta information likhte hai.
                ex:- website ko title dena ho
.shortcut:-

    ->(! + tab)                   --> to write starter code
    ->(h1 + enter)                --> to write h1 tag
    ->(alt+shift+down arrow)      --> to copy same line
    -><b> tag is used to bold the Text.
    -><strong> is also used to bold the tag but it is stronger than <b> tag.
    -><i> tag is used for italic.
    -><em> is same as <i> tag.
    -><del> is  used for line through on some Text.
    -><sub> -- 
    -><sup> -- to write power.
    -><mark>-- used to highlight.

-> alt + press 
-> br *2  --> write 2 times br tag 
-> class ka name css me likhne se phle (.) lagate hai.
-> id ka name css me likhne se phle (#) lagate hai.
-> class same ho sakte hai. but id hmesa unique rahegi.--> ye convention hai

-> For line drawing use <hr> tag.